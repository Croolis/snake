Все сообщения имеют вид
{
  "message": <тип сообщения>,
  "data": <дополнительная информация, если она есть>
}

Типы сообщений:
1)  "your name"
    Посылается в начале игры, устанавливает имя игрока.
    Дополнительная информация: строка с именем игрока.

2)  "game start"
    Посылается после сообщения с именем, сообщает имена и цвета других игроков.
    Дополнительная информация: массив вида
      [
        {
          "username": <имя пользователя>,
          "color": [<R>, <G>, <B>]
        },
        ...
      ]
3)  "move"
    Посылается на каждой итерации игры, если игра не в состоянии дуэли.
    Дополнительная информация: массив вида
      [
        <объект>: <состояние>,
        ...
      ]
      Типы объектов:
      I.    "apple"
            Яблоко
            Состояние: массив вида [<x>, <y>]
      II.   "mouse"
            Мышка
            Состояние: массив вида [<x>, <y>]
      III.  "player"
            Игрок
            Состояние: объект вида
              {
                "name": <имя игрока>,
                "snake": [[<x>, <y>], [<x>, <y>], ...]
              }
4)  "cut"
    Посылается когда один игрок кусает другого.
    Дополнительная информация: объект вида
      {
        "predator": <имя хищника>,
        "victim": <имя жертвы>
      }
5)  "apple eaten"
    Посылается когда игрок ест яблоко.
    Дополнительная информация: строка с именем игрока.
6)  "mouse eaten"
    Посылается когда игрок есть мышку.
    Дополнительная информация: строка с именем игрока.
7)  "died"
    Посылается когда игрок умирает.
    Дополнительная информация: строка с именем игрока.
8)  "game over"
    Посылается в конце игры перед разрывом соединения.
    Дополнительная информация: строка с именем победителя.
9)  "duel"
    Посылается на каждой итерации дуэли.
    Дополнительная информация: объект вида
      {
        "player1": <имя первого дуэлянта>,
        "player2": <имя второго дуэлянта>,
        "pow1": <сила первого дуэлянта>,
        "pow2": <сила второго дуэлянта>,
        "orient1": <кнопка, которую должен нажать первый дуэлянт>,
        "orient2": <кнопка, которую должен нажать второй дуэлянт>
      }
10) "duel ended"
    Посылается в конце дуэли.
    Дополнительная информация: объект вида
      {
        "winner": <имя победителя>,
        "loser": <имя проигравшего>
      }

